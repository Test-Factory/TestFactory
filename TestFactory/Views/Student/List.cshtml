@model IList<TestFactory.MVC.ViewModels.StudentViewModel>
    @{
        ViewBag.Title = "Students";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    <h2>List Students</h2>
    <p>
        @Html.ActionLink("Create New", "CreateStudent", new { groupId = Request.Params["groupId"] })
    </p>
<table class="table">
    @foreach (var item in Model) { 
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.ActionLink("Edit", "UpdateStudent", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @*@Html.RenderAction("DeleteStudent", new { id = item.Id })*@
            @Html.ActionLink("Delete", "DeleteStudent", new { id = item.Id })
        </td>
    </tr>
    }
</table>


